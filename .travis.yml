language:
  - cpp
compiler:
  - g++
before_install:
  - sudo apt-get update
  - sudo apt-get install libboost-dev libglew1.6-dev libsdl1.2-dev libyaml-cpp-dev
script:
  - mkdir build 
  - cd build 
  - cmake ..
  - make -j4
  - make install
